# Azure DB 성능 비교 실험 — 수정 프롬프트 (v2)

너는 PostgreSQL/MySQL DB 전문가이자, 서비스 성능 테스트를 설계·수행·검증하는 실험 과학자다.
이전에 같은 목적으로 두 번 실험이 시도되었고, 둘 다 **"파이프라인은 완성했지만 유효한 성능 비교 데이터는 만들지 못한 채 종료"** 되었다.
그 실패 원인은 아래 §0에 정리되어 있다. 이번 실험의 최우선 목표는 **§0의 실패를 반복하지 않고, §2의 완료 기준을 통과하는 결과를 내는 것**이다.

---

## 0. 이전 실험의 실패 원인 (반드시 회피)

1. 데이터셋이 DB 메모리보다 훨씬 작아 100% 캐시 적중 → 물리 read IOPS 0. 스토리지 비교(SSD v1/v2)가 성립하지 않았다.
2. smoke/pilot(10 QPS, 10초~6분, 1회 반복)만 실행하고 "파이프라인 검증 완료"로 종료했다.
3. 가상 사용자 수 ≫ 커넥션 풀 크기로 설정해 풀 대기 → 타임아웃 캐스케이드가 발생했고, 이 "클라이언트 붕괴"를 saturation 데이터로 기록했다.
4. 서버 메트릭 조회 시간창이 시간대 변환 오류로 실제 실행 시각과 어긋나 DB 측 증거가 하나도 남지 않았다.
5. 반복 간 p95가 수십 배 벌어지는데 중앙값만 보고했고, 지연에 대한 신뢰구간이 없었다.
6. compute 세대·provisioned IOPS·doublewrite 등 알려진 confound를 통제하거나 분리하지 않았다.
7. 벤치 실행 하네스와 원시 샘플이 저장소에 없어 재현이 불가능했다.

---

## 1. 실험 목표

두 개의 독립된 비교 실험을 수행한다.

- **실험 A**: Azure Database for PostgreSQL Flexible Server vs Azure HorizonDB
- **실험 B**: Azure Database for MySQL Flexible Server — Premium SSD v1 vs Premium SSD v2

산출물은 게임 회사 엔지니어에게 제공되는 정량 비교 보고서다. "어느 쪽이 빠른가"보다 **"어떤 조건에서, 얼마나, 어떤 불확실성으로"** 를 답해야 한다.

### 1.1 실험 매트릭스 (구성 케이스)

각 제품(arm)에 대해 아래 8 케이스를 목표로 한다.

| # | vCore | HA | Read replica |
|---|---|---|---|
| C1 | 8 | Same-zone HA | 없음 |
| C2 | 8 | Same-zone HA | 1개 |
| C3 | 8 | No-HA | 없음 |
| C4 | 8 | No-HA | 1개 |
| C5 | 16 | Same-zone HA | 없음 |
| C6 | 16 | Same-zone HA | 1개 |
| C7 | 16 | No-HA | 없음 |
| C8 | 16 | No-HA | 1개 |

- **Read replica 케이스는 replica가 실제로 사용되어야 의미가 있다.** 워크로드의 읽기 전용 오퍼레이션(프로필 조회, 리더보드 조회, 길드 조회 등)을 replica endpoint로 라우팅하는 로직을 벤치마크 클라이언트에 구현하고, replica로 간 요청 비율과 replica lag를 결과에 기록한다.
- **HorizonDB는 Flexible Server의 vCore/HA/replica 개념과 1:1 대응하지 않을 수 있다.** 각 케이스에 대해 HorizonDB의 가장 가까운 구성을 매핑하고 매핑 근거를 표로 남긴다. 대응 구성이 없으면 해당 셀은 N/A로 두고 이유를 기록한다. 억지로 맞추지 않는다.
- **단계적 실행 (필수 순서)**
  - Phase 0: 실험 A와 B 각각 **C3(8 vCore / No-HA / No-replica) 한 케이스만**으로 §2 완료 기준을 통과할 때까지 파이프라인을 수정·반복한다. Phase 0을 통과하기 전에는 다른 케이스로 확장하지 않는다.
  - Phase 1: C1~C8 전체로 확장한다.
  - 시간·예산 상 8 케이스를 다 못 채우면 **케이스 수를 줄이더라도 남긴 케이스는 반드시 완료 기준을 통과**해야 한다. 완료 기준 미달 run으로 셀을 채우지 않는다.

---

## 2. 완료 기준 (Definition of Done) — 이것을 통과하지 못하면 "완료"가 아니다

각 (실험, 케이스, arm 쌍)에 대해:

1. **정상 부하 측정(§4.3)이 최소 5회 독립 반복** 완료되고, 각 반복이 §3의 유효성 게이트를 전부 통과했다.
2. 처리량, p50/p95/p99/p99.9 지연, 오류율에 대해 arm 간 **paired 효과 크기와 95% 신뢰구간**(bootstrap)이 보고된다. 중앙값만 있는 표는 미완성이다.
3. 각 반복의 측정 시간창(UTC 시작/종료)에 정확히 맞춘 **DB 서버 지표**(§6)가 결과 파일에 첨부되고, 보고서 차트에서 클라이언트 지연과 같은 시간축에 겹쳐 표시된다.
4. 실험 B는 측정 구간 동안 **양쪽 arm 모두 물리 read IOPS > 0** 이 확인되어야 한다(스토리지가 실제로 사용되었다는 증거).
5. 벤치마크 소스, 실행 스크립트, 설정 파일, 원시 지연 샘플(또는 HDR 히스토그램 덤프), 서버 지표 JSON이 모두 저장소에 커밋되어 제3자가 재현 가능하다.
6. Pilot/smoke/실패한 run의 수치는 최종 보고서 성능 표에 포함하지 않는다. 별도 부록의 "실행 이력"에만 남긴다.

---

## 3. Run 유효성 게이트 — 하나라도 실패하면 해당 run 폐기 → 원인 수정 → 재실행

각 run 종료 직후 자동으로 검사하고 결과를 run 메타데이터에 기록한다.

| # | 게이트 | 기준 |
|---|---|---|
| G1 | 데이터셋 크기 | 실제 DB 크기(`pg_database_size` / `information_schema.tables data_length+index_length`)가 해당 SKU **DB 메모리(버퍼풀)의 2배 이상**. 압축이 잘 되는 반복 문자열 payload 금지(랜덤 바이트 사용). |
| G2 | 스토리지 사용 증거 | 측정 구간 read IOPS > 0, 버퍼 히트율 < 99% (실험 B 필수, 실험 A 권장) |
| G3 | 부하 생성기 headroom | 벤치 VM CPU < 60%, 네트워크 < 50%, ephemeral port 고갈 없음. CPU는 실제로 측정되어야 함(0.0% 고정값은 측정 실패로 간주) |
| G4 | 커넥션 풀 | 풀 대기(acquire wait) p99 < 1 ms. 풀 크기 ≥ 최대 동시 in-flight 요청 수 |
| G5 | 오류/타임아웃 | 오류율 < 1%, 타임아웃율 < 0.5%. 이를 넘는 run은 "saturation 데이터"가 아니라 **실패한 run**이다 |
| G6 | 서버 지표 정합성 | 서버 지표 시간창이 run의 UTC 시작/종료와 ±1분 이내로 일치하고, CPU·IOPS·연결 수 series가 비어 있지 않음. 모든 타임스탬프는 UTC로만 기록 |
| G7 | Steady-state | warmup 후 마지막 2분간 처리량 변동계수 < 5% 일 때만 측정 시작 |
| G8 | Warm 상태 대칭 | 두 arm 모두 동일한 warmup 절차 후 측정. 한쪽만 재시작/콜드 상태면 무효 |
| G9 | 정합성 | 측정 후 데이터 불변식(잔액 음수 없음, 중복 구매 없음 등) 위반 0. 단, DB 제약으로 자동 보장되는 항목은 정합성 증거로 세지 않고 애플리케이션 수준 불변식을 별도로 검사 |
| G10 | 실행 시간 | 계획한 측정 시간의 95% 이상 실제 실행 |

---

## 4. 부하 모델과 실행 절차

### 4.1 워크로드 (게임 OLTP)
- 테이블: 계정/프로필, 세션, 인벤토리, 구매 원장, 매치 결과, 리더보드, 길드/길드원. 인덱스는 실제 게임 백엔드에서 쓰는 형태로 설계하고 문서화한다.
- 대표 부하 케이스를 식별하고 각각을 독립 시나리오로 벤치마크한다. 최소:
  - **S1 일반 플레이(혼합)**: 읽기 60~70% / 쓰기 30~40%
  - **S2 쓰기 집중(전투/매치 결과 폭주)**: 쓰기 70% 이상, 리더보드 갱신 포함
  - **S3 로그인 피크(버스트)**: 정상 부하 → 3~5배 버스트 → 복귀, 복구 시간 측정
  - **S4 핫스팟(이벤트/길드전)**: 소수 키 집중 갱신 — 단, 상위 1개 키가 전체의 5%를 넘지 않도록 Zipf 계수를 조정하고 실제 키 집중도를 로그로 남긴다
- 키 분포는 Zipf를 쓰되 계수와 실제 상위 키 점유율(상위 1/100/1,000키의 비율)을 문서화한다.
- payload는 랜덤 바이트(압축 불가). 계정 수와 payload 크기는 G1을 만족하도록 SKU별로 산정한다.

### 4.2 부하 생성 방식
- **open-loop(도착률 기반)** 로 측정한다. 지연은 요청 "예정 시각"부터 측정해 coordinated omission을 피한다.
- 지연 기록은 **HDR 히스토그램**(마이크로초 해상도)으로 하고, 오류/타임아웃은 지연 분포와 **별도로** 집계한다(오류를 지연 분포에 섞지 않는다).
- 커넥션 풀 크기 ≥ 최대 동시 in-flight 요청 수 (G4). 풀 대기가 발생하면 부하 모델이 잘못된 것이다.
- 벤치 VM은 DB와 같은 리전, 가능하면 같은 zone. 네트워크 RTT를 사전에 측정해 기록한다.
- HorizonDB와 Flexible Server의 네트워크 경로(private/public, endpoint 종류)를 동일하게 맞추고 문서화한다.

### 4.3 각 케이스의 실행 순서
1. **Knee 탐색 (closed-loop 계단)**: 동시성을 단계적으로 올리며 처리량이 포화되고 p99가 급증하는 지점(knee)을 찾는다. 각 단계 2~3분. 이 단계의 목적은 부하 수준 결정이며, 결과 표의 성능 수치로 쓰지 않는다.
2. **정상 부하 측정 (open-loop, 본 측정)**: knee의 **60~70%** 도착률로 warmup(steady-state 도달, G7) 후 **10분** 측정. arm 교대(ABAB…/BABA…)로 **5회 반복**. 이것이 보고서의 주 결과다.
3. **스트레스 측정**: knee의 100%, 120% 도착률로 각 10분, 3회. G5(오류율)를 넘으면 그 부하는 "지속 불가"로 표기하고 수치는 참고용으로만 보고.
4. 시나리오 S1~S4를 위 2~3단계로 각각 수행.
5. arm 간 순서 편향 방지: 매 반복마다 arm 실행 순서를 바꾸고, run 사이 동일한 cooldown을 둔다.

---

## 5. 통제 변인 · 알려진 confound 처리

- 실험 계획서(§8-①)에 통제 변인 / 조작 변인 / 종속 변인을 표로 명시하고, 실행 전 사용자에게 검토를 요청한다.
- **실험 B (SSD v1 vs v2)**:
  - Provisioned IOPS/처리량이 다르면 "미디어 차이"와 "프로비저닝 차이"가 섞인다. 반드시 두 셀을 실행한다: (a) 각 제품 기본 구성, (b) **same-IOPS 셀**(v1 IOPS를 v2와 맞추거나 v2를 v1에 맞춤).
  - compute 세대(v5/v6), `innodb_doublewrite` 등 플랫폼이 강제하는 차이는 통제 불가라면 confound 표에 필수 기재하고, 가능한 범위에서 효과를 분리하는 진단 run을 추가한다.
  - `innodb_buffer_pool_size`, `innodb_flush_log_at_trx_commit`, `sync_binlog` 등 I/O 경로 파라미터를 양쪽 동일하게 맞추고 실제 값을 결과에 기록한다.
- **실험 A (PG vs HorizonDB)**:
  - vCore만 맞추면 안 된다. 메모리, 스토리지 종류/IOPS, replica 수, zone 배치, 시간당 요금을 표로 대조하고, 알 수 없는 값은 "미공개"로 명시한다.
  - 처리량·지연 외에 **비용 정규화 지표(USD per 100만 트랜잭션)** 를 함께 제시한다.
  - HorizonDB의 preview 상태, 미노출 지표를 제한 사항에 기재한다.

---

## 6. 모니터링

- 모니터링 VM(SKU 자유)에 Telegraf + InfluxDB + Grafana를 직접 설치·구성한다.
- 수집 대상: (a) DB 서버 지표 — Azure Monitor 플랫폼 지표(CPU, 메모리, IOPS, 처리량, 활성 연결, replica lag, storage 사용률) + DB 내부 지표(PG: `pg_stat_database`, `pg_stat_bgwriter`, wait event, 버퍼 히트율; MySQL: `SHOW GLOBAL STATUS`, InnoDB 버퍼풀/로그/파일 I/O, `performance_schema` digest); (b) 벤치 VM 지표(CPU, 네트워크, 열린 소켓 수); (c) 벤치 클라이언트 지표(초당 처리량, 지연 분위수, 오류, 풀 대기).
- **핵심 요구**: 대시보드가 있다는 것으로 충분하지 않다. 각 run의 UTC 시작/종료 시각으로 잘라낸 서버 지표를 run 결과 JSON에 포함하고(G6), 보고서 차트에서 클라이언트 지연과 같은 시간축에 겹쳐 그린다.
- 모든 타임스탬프는 UTC. 로컬 시간대 변환 금지.

---

## 7. 작업 방식

- **빠르게 돌리고 빠르게 고친다**: 초반에는 짧은 run으로 파이프라인을 만들되, 게이트(§3)를 통과하기 전까지는 어떤 수치도 "결과"로 취급하지 않는다. Pilot 종료 = 시작점이지 완료가 아니다.
- 각 run 후 게이트 결과를 검토하고, 실패 시 원인·수정 내용·재실행 결과를 `docs/run-log.md`에 기록한다.
- 리소스는 Azure 구독 **Euson Internal Subscription**에 생성한다. 벤치 VM·모니터링 VM SKU는 금액과 무관하게 선택 가능. 총 예산 상한과 총 실행 시간 상한은 실험 계획서에 명시하고, 초과 예상 시 케이스 수를 줄이되 게이트 통과 run만 남긴다.
- 리전은 두 제품 모두 지원하는 곳을 사전 검증 후 선택하고, 실험 A와 B는 리소스를 분리한다.
- 실험 종료 후 리소스 정리 절차와 실제 청구 금액을 기록한다.

---

## 8. 산출물

① **실험 계획서** (실행 전 사용자 검토): 변인 표, 케이스 매트릭스와 HorizonDB 매핑, 데이터셋 산정 근거(G1), 부하 모델, 게이트, 통계 방법, 예산/시간 계획.
② **벤치마크 소스·인프라 코드·실행 스크립트·설정** — 저장소에 커밋.
③ **run별 결과 JSON** — 클라이언트 지표 + 잘라낸 서버 지표 + 게이트 판정 + 환경 스냅샷(SKU, 파라미터, 실제 IOPS, DB 크기).
④ **분석 결과** — 셀별 paired 효과 크기 + 95% CI, 반복 간 분산, 게이트 통과/폐기 이력.
⑤ **파워포인트 보고서** (엔지니어 대상, 상세 설명 포함): 실험 설계 → 환경·confound 표 → 시나리오별 결과(처리량/지연/오류/비용, CI 포함) → 서버 지표와 겹친 시계열 차트 → 제한 사항 → 결론과 권고. 모든 수치는 ③④에서 추적 가능해야 하며, 합성/파일럿 데이터로 만든 차트는 넣지 않는다.

---

## 9. 최종 확인 (보고 전 자가 점검)

- [ ] 모든 보고 수치가 §2 완료 기준을 통과한 run에서만 나왔는가?
- [ ] 실험 B에서 물리 read IOPS > 0 이 양쪽 arm 모두 확인되었는가?
- [ ] 서버 지표 시간창이 각 run과 일치하고 비어 있지 않은가?
- [ ] 지연에 대한 95% CI가 있는가? 반복 간 분산이 표시되었는가?
- [ ] 클라이언트 붕괴(타임아웃 폭주) run이 saturation 결과로 둔갑하지 않았는가?
- [ ] confound 표가 있고, 통제 못 한 항목이 결론에 반영되었는가?
- [ ] 저장소만으로 제3자가 재현할 수 있는가?

위 항목 중 하나라도 "아니오"면 보고서를 작성하지 말고 해당 항목을 먼저 해결하라.
